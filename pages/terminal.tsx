import React from 'react';
import api from '../lib/api';

const Terminal = () => {
    return (
        <div>
            <h1>Terminal</h1>
        </div>
    );
};

export default Terminal; 